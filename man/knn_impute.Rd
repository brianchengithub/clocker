% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation.R
\name{knn_impute}
\alias{knn_impute}
\title{kNN imputation with zero-shot fallback to reference probe means}
\usage{
knn_impute(
  betas,
  reference_path = NULL,
  k = 10L,
  zero_shot_threshold = 0.1,
  distance = c("pearson", "euclidean"),
  verbose = TRUE
)
}
\arguments{
\item{betas}{Beta matrix (probes x samples). Missing values = NA.}

\item{reference_path}{Optional path to reference_betas.rds. If NULL,
resolved by \code{resolve_reference_betas_path()}.}

\item{k}{Number of nearest in-batch neighbours used for kNN.}

\item{zero_shot_threshold}{Per-sample fraction of probes missing above
which kNN is bypassed and reference means are used directly.}

\item{distance}{Distance metric: "pearson" (1 - r) or "euclidean".}

\item{verbose}{Print progress}
}
\value{
List with \code{betas} (imputed) and \code{sample_info} (per-sample frame).
}
\description{
kNN imputation with zero-shot fallback to reference probe means
}
\keyword{internal}
